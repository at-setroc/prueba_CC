<nav id="top-bar" class="navbar">
    <div class="container-fluid px-4">
        <a class="navbar-brand" href="{{ path('app_homepage') }}">
            <img src="{{ asset('assets/logo.svg') }}" alt="" width="auto" height="auto">
        </a>
        <a id="btn-logout" href="{{ path('app_logout') }}">
            {{"Cerrar sesión"|trans}}
        </a>
    </div>
</nav>